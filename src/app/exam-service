import { Injectable } from '@angular/core';
import { Exam } from './exam';
import { EXAMS } from './mock-exams';

@Injectable()
export class HomeworkService
{
	getHomeworks(): Promise<Exam[]> 
	{
		return Promise.resolve(EXAMS);
	}
}